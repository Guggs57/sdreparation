<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sdreparation" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Ne casse pas si favicon absent -->
    <%= favicon_link_tag "favicon.ico", rel: "icon", type: "image/x-icon", skip_pipeline: true rescue nil %>
    <%= favicon_link_tag "apple-touch-icon.png", rel: "apple-touch-icon", skip_pipeline: true rescue nil %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>


  </head>
  <body>
    <%= yield %>
  </body>
</html>
