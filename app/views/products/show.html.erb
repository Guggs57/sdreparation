<h1><%= @product.name %></h1>
<p><%= @product.description %></p>
<p><strong><%= number_to_currency(@product.price, unit: "€") %></strong></p>
<%= image_tag @product.image_url, alt: @product.name, style: "max-width: 300px;" if @product.image_url.present? %>

<%= link_to "Retour à la boutique", products_path %>
